# DATAJOT-MODELS

This repos contains all models used as template in the datajot application.
All models are bundled together and hosted on github-pages at models.datajot.app

You can find all models definition of all models in that repository in MODELS.MD.
All bundle are defined in BUNDLES.MD
